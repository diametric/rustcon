text := import("text")
fmt := import("fmt")

// Split out the "30 FPS" string to get the value.
parts := text.fields(_INPUT)

if is_undefined(_GLOBALS["test"]) {
    _GLOBALS["test"] = 0
} else {
    _GLOBALS["test"] += 1
}

fmt.printf("TEST TEST TEST test contains %d\n", _GLOBALS["test"])

_BUCKET := "sixty_days"
_MEASUREMENTS := [format("serverfps,servertag=%s fps=%s", _TAG, parts[0])]
